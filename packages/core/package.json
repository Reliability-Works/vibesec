{
  "name": "@reliability-works/core",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "pnpm build && node --test test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fast-glob": "^3.0.0",
    "picomatch": "^4.0.0",
    "yaml": "^2.0.0",
    "zod": "^3.0.0"
  }
}
